<div class="chat-message__container chat-message__container_{{ side }} {{#if new}}chat-message__container_new{{/if}}">
    {{#if avatar}}<img class="chat-message__avatar" src="{{ avatar }}" alt="">{{/if}}
    <div class="chat-message chat-message_{{ side }} chat-message_last_{{ side }} {{ style }}">{{ body }}</div>
</div>
