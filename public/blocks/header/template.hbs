<nav class="header">
    <div class="header__container container">
        <div class="header__logo gradient-text">EVENTUM</div>
        <div class="header__manage">
            {{#each buttons}}
                <a class="header__item" href="{{ link }}">{{ name }}</a>
            {{/each}}
        </div>
    </div>
</nav>
