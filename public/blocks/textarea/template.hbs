<textarea class="textarea"
          name="{{ name }}"
          rows="{{ rows }}"
          placeholder="{{ placeholder }}"
          {{# if data_bind_event}}data-bind-event="{{ data_bind_event }}"{{/if}}>{{ body }}</textarea>